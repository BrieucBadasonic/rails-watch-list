<div class="container">
  <h1 class="text-center mt-5">Create your list</h1>
  <hr>
  <%= simple_form_for(@list) do |f| %>
    <%= f.input :name %>
    <%= f.input :list_url, label: "Enter a URL for the picture of the list" %>
    <%= f.submit %>
  <% end %>

</div>
